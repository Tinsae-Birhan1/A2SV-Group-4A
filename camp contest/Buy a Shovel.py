def BuyShovel():
    a, b= [int(x) for x in input().split()]
    sums = 0
    for i in range(a):
    	sums += a 
    	if sums %10 == 0 or sums%10 ==b:
    		print(i)
    		break
    	else:
    		continue